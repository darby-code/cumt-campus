<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doug.ssm.dao.UserDao">
    <select id="queryStudentBy" resultType="com.doug.ssm.entity.Student">
        SELECT student_id, student_name, password, college_id, college_name
        FROM student
        WHERE student_id = #{studentId}
    </select>
    <select id="queryTeacherBy" resultType="com.doug.ssm.entity.Teacher">
        SELECT teacher_id, teacher_name, password
        FROM teacher
        WHERE teacher_id = #{teacherId}
    </select>
    <select id="queryAllTeachers" resultType="com.doug.ssm.entity.Teacher">
        SELECT teacher_id, teacher_name, password
        FROM teacher
    </select>
    <select id="queryAllStudents" resultType="com.doug.ssm.entity.Student">
        SELECT student_id, student_name, password, college_id, college_name
        FROM student
    </select>
    <select id="queryCollegerBy" resultType="com.doug.ssm.entity.Student">
        SELECT student_id, student_name, password, college_id, college_name
        FROM student
        WHERE college_id = #{collegeId}
    </select>
    <update id="updateTeacherPassword" parameterType="com.doug.ssm.entity.Teacher">
        UPDATE teacher
        SET password = #{password}
        WHERE teacher_id = #{teacherId}
    </update>
    <update id="updateStudentPassword" parameterType="com.doug.ssm.entity.Student">
        UPDATE student
        SET password = #{password}
        WHERE student_id = #{studentId}
    </update>
    <delete id="deleteStudentBy">
        DELETE FROM student WHERE student_id = #{studentId}
    </delete>
    <delete id="deleteTeacherBy">
        DELETE FROM teacher WHERE teacher_id = #{teacher_id}
    </delete>
</mapper>