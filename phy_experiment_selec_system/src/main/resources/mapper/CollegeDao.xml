<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.doug.ssm.dao.CollegeDao">
    <select id="queryCollegeNameBy" resultType="String">
        SELECT college_name FROM college WHERE college_id = #{collegeId}
    </select>

    <select id="queryAllColleges" resultType="com.doug.ssm.entity.College">
        SELECT college_id, college_name FROM college
    </select>
</mapper>